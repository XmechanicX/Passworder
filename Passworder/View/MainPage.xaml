<Page x:Class="Passworder.View.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Passworder.View"
      xmlns:vm="clr-namespace:Passworder.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage"
      Background="White">

    <Page.DataContext>
        <vm:vmMainPage/>
    </Page.DataContext>

    <StackPanel>
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>
            <StackPanel x:Name="ListWebsite" Grid.Column="0" Background="Gray">
                <ListBox ItemsSource="{Binding Items}" 
                         SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                         DisplayMemberPath="Title"
                         Height="200"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Add" Command="{Binding AddCommand}" Width="75" Margin="0,0,8,0"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Width="75"/>
                </StackPanel>
            </StackPanel>
            <StackPanel x:Name="Info" Grid.Column="1">
                <TextBox Text="{Binding SelectedItem.Website, Mode=TwoWay}" Foreground="Black" Margin="10" Height="30"/>
                <TextBox Text="{Binding SelectedItem.Title, Mode=TwoWay}" Foreground="Black" Margin="10" Height="30"/>
                <TextBox Text="{Binding SelectedItem.Login, Mode=TwoWay}" Foreground="Black" Margin="10" Height="30"/>
                <TextBox Text="{Binding SelectedItem.Password, Mode=TwoWay}" Foreground="Black" Margin="10" Height="30"/>
                <TextBox Text="{Binding SelectedItem.Notes, Mode=TwoWay}" Foreground="Black" Margin="10" Height="30"/>
            </StackPanel>
        </Grid>
    </StackPanel>
</Page>
